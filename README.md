# sheeran

A simple Flutter app for browsing Ed Sheeran's albums.

## Testing

Be sure to run:

    flutter pub clean && flutter pub get

To run unit tests, connect a device, then run:

    flutter test test/unit_tests.dart

To run integration tests:

    flutter test integration_test/ui_test.dart
